CREATE TABLE pagos (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    pedido_id BIGINT NOT NULL, metodo VARCHAR(40) NOT NULL, monto INT NOT NULL, estado VARCHAR(30) NOT NULL, fecha_pago TIMESTAMP NOT NULL
);
