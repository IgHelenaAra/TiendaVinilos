CREATE TABLE despachos (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    pedido_id BIGINT NOT NULL, direccion VARCHAR(180) NOT NULL, comuna VARCHAR(80) NOT NULL, estado VARCHAR(30) NOT NULL, fecha_programada DATE NOT NULL
);
